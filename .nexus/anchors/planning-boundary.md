---
concept: planning-boundary
source_sha: bea350eead91be2c0b9e90c6cf002422cc1a12cc
generated: 2026-09-20
---

<!-- DERIVED — regenerated by /nxs.distill on every drain touching this concept.
     Never hand-edit; stale anchors are rebuilt, not fixed. -->

# Code Anchors: Planning Boundary

- `src/roadmap.ts` — whether any member is planned, read off the stated kind, and the refusal a phase that reads stories gives a roadmap with none (#90)
- `src/plan-commit.ts` — the members that contributed no slice, in the roadmap's own order, and the list written into the plan after its slices (#90)
- `src/workbook-plan.ts` — the list read back off the committed plan field by field, and refused when an entry carries anything beyond its number and title (#90)
- `src/plan-approval.ts` — the block the gate prints last, naming each member past the boundary and how much of the roadmap the plan covers (#90)
- `src/workbook-cli.ts` — the extraction refusal for a roadmap with nothing planned, and the boundary handed to the gate's print (#90)
- `src/concept-extraction.ts` — the same refusal repeated where the draft is written (#90)
- `src/plan-commit.spec.ts` — pins the recorded list, the omission on a fully planned roadmap, and the round trip through a teaching session (#90)
- `src/plan-approval.spec.ts` — pins the printed block and the unchanged print on a fully planned roadmap (#90)
- `src/concept-extraction.spec.ts` — pins the refusal given before any story is read (#90)
- `components/commands/nxsx.teach-plan.md` — the phase text: what the chain runs over, what the plan records, and what the gate shows the reviewer (#90)
- `src/planning-boundary.ts` — the first recorded entry read back as the epic a learner plans next (#96)
